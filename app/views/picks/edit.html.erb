<h2>Pick for <%= @pick.slot_name  %></h2>

<%= form_for @pick do |f| %>
  <%= f.label :entrant_id, "Winner" %>
  <%= f.collection_select :entrant_id, @entrants, :id, :player_name, prompt: "Select Winner" %>
  <%= f.submit "Make Pick" %>
<% end %>
