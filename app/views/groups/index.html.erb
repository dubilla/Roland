<h2>Groups</h2>

<% if @groups.length > 0 %>
  <ul>
    <% @groups.each do |group| %>
      <li>
        <%= group.name %>
      </li>
    <% end %>
  </ul>
<% else %>
  <span>There are no groups</span>
<% end %>
<%= link_to 'Create a group', new_group_path %>
